import { Inter, Lusitana, Cinzel} from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const cinzel = Cinzel({ subsets: ['latin'] });


export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
  });